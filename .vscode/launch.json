{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Preview themes (Extension Development Host)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
    }
  ]
}
